# Progress

Tracks what has been pushed. See HANDOFF.md (untracked, machine-local) for in-flight work.

- 2026-09-13 per-environment fan-out (#51): spec, plan, implementation on branch feat/per-environment-fanout; follow-up #55 completion monitoring.
- 2026-09-14 fan-out merged (#57, 916d77a) and deployed to EKS; TMI addon tmi-tf-wh registered (param `environments`).
- 2026-09-15 #58 (62fb43a): webhook allow-lists trigger events (TMI 1.10.7 `metadata.updated` caused a self-feeding job loop) and redacts token headers; deployed to EKS with the rotated OpenAI key. Verification of `environments=all` still pending (see HANDOFF.md).
- 2026-09-15 #59 (58960e2): event type read from X-Webhook-Event header (#58 had ignored all real deliveries); deployed to EKS.
- 2026-09-16 #60 (55abf05, image e89d9b4): DFD edges emitted as shape `flow` (TMI rejected `edge`, diagrams were left empty); threat text escapes TMI's XSS regexes (`on\w+\s*=` rejected prose like `deletion_protection = true`, tmi #885 filed). Deployed to EKS; verified on tmi-ux (24-cell diagram, 7/7 threats). oci-private run still fails at phase 1 (16000 output-token ceiling, #53).
- 2026-09-16 #61 (f4d3607, image e06f870): LLM calls stream via LiteLLM; output cap 64000 tokens, timeout 1200s (supported models: Opus 4.8+, Fable/Mythos 5+, GPT-5.6 sol/terra+, GPT-6 Astra, Grok 4.6+; all >=128k output). Deployed to EKS. oci-private run succeeded: phase 1 16734 output tokens (135 components), 110-cell DFD, 20/20 threats, ~$9 LLM for both jobs.
- 2026-09-16 switched deployment to anthropic/claude-mythos-5 (key from ~/.keys/MYTHOS_API_KEY; the key does not see mythos-5.1). aws-public run on Mythos: tmi 47-cell DFD, 22/24 threats; tmi-ux 16-cell DFD, 10/11 threats; ~$29 LLM. The 3 lost threats had mitigations over TMI's 1024-char limit.
- 2026-09-16 #62 (18155e1): threat name/description/mitigation clamped to schema max lengths (256/2048/1024) at a word boundary. Deployed to EKS before the oci-private Mythos run.
- 2026-09-16 oci-private run on claude-mythos-5 (image 18155e1): tmi/oci-private 129 components, 72-cell DFD, 28/28 threats; tmi-ux 16-cell DFD, 12/12 threats; zero validation errors; ~$40 LLM.
- 2026-09-16 bot credential rotated to an addon-linked one (tmi #883, TMI 1.12.3): worker writes no longer echo metadata.updated to the webhook (verified: 42 note writes, 1 delivery). PR #63 open: drop the on*= escape now that tmi #885 shipped. No image push yet.
- 2026-09-16 comparison runs on aws-public + oci-private (image 18155e1, TM wiped first). Mythos (claude-mythos-5): 28 + 28 findings, 51- and 60-cell DFDs, 55/56 threats, ~$38. Daybreak (gpt-daybreak-blue-latest): 22 + 30 findings, 97- and 103-cell DFDs, 51/52 threats, ~$14. One threat lost per run to #64 (apostrophe in threat name fails the client name regex). Deployment left on openai/daybreak. PR #66 fixes #65 (repo name rstrip bug); not deployed.
- 2026-09-16 #63 (drop on*= escape, tmi #885 shipped), #66 (repo name rstrip(".git") bug: ericfitz_tm -> ericfitz_tmi), #67 (#64 threat-name characters; #56 child status notes named per repo and environment) merged; image 1d70eeb deployed to EKS on openai/gpt-daybreak-blue-latest. #53 narrowed to validate-and-retry backlog.
- 2026-09-17 #68 (42586c9): phase 1 retries once on truncated (finish_reason=length) or unparseable JSON, then fails the child job loudly (status note "Analysis failed: ...", success=false) instead of "Analysis complete" (#53). Deployed to EKS (pod ready, health 200). Old bot credential e87017a6 deleted by Eric; the worker uses the addon-linked credential ff7db4f2.
- 2026-09-18 #69 (d6309b9): invocation tracking: completion callback (#55) and duplicate-trigger drop (#52). Deployed to EKS (pod ready, health 200). Prod tests on TM 02909291: the parent opens the invocation and a second trigger is dropped as a duplicate (webhook 200). Completion does not work yet: TMI returns 500 for note metadata keys containing a colon, so child marks never land (#71, reported to the tmi agent); `append_status_line` never writes because the notes list item has no content (#70). The OpenAI key in the cluster Secret is rejected (401 invalid_api_key), so no analysis ran; success path, debounce and `environments=all` tests are still to run.
- 2026-09-18 #72 (8707d12): #70 fixed (append_status_line fetches the full note; list items carry no content); watchdog survives mark failures (prod 15:09Z: a TMI 500 on the child mark skipped the close and the terminal callback); child marks renamed `tf_child_<job_id>`, limited to `[A-Za-z0-9_-]`, because TMI 1.13.12 returns 500 on bulk metadata keys containing `:` or `.` (#71, probed). Deployed to EKS with llm_model=gpt-5.6-cyber and the rotated OpenAI key; rollout ok, health 200. Prod invocation tests (a)-(d) postponed until the TMI server fix lands (Eric).
- 2026-09-22 prod invocation tests on TM 02909291 (TMI 1.13.15, image 8707d12; TM wiped first). gpt-5.6-cyber rejects chat/completions ("Use the v1/responses endpoint"), so llm_model is now `openai/responses/gpt-5.6-cyber` (LiteLLM Responses bridge; the name must carry the `openai/` prefix because `api_key.py` keeps any model containing `/` as-is). Config change only, no image. (a) aws-public: 16/16 threats, 1 diagram, "Invocation complete: 1 succeeded, 0 failed", child mark and tf_open=false landed (no 500), ~$5.5 LLM; #55/#70/#71/#72 verified. (b) invoke during (a): dropped at the webhook, "Trigger addon.invoked ignored: analysis already running" on the parent note (#52). (c) second invoke 16 s after the first: dropped (TMI itself now returns 429 duplicate_invocation for back-to-back invokes, so the drop happened past its guard). (d) environments=all skipped (aws-public only). Follow-up #73: artifact metadata create on re-used notes/diagram returns 409 "Metadata key(s) already exist".
- 2026-09-22 #74 (ac776cf): abort mechanics for invocations (#54 part 1, local only): cancel event stops the analysis at the next phase boundary (`AnalysisAborted`); `WorkerPool.abort(invocation_id, reason)` cancels running jobs, drops queued children, closes the invocation ("Aborted: <reason>") and sends a `failed` callback. No external trigger yet: PR 3 wires it to TMI delivery cancel (tmi #913, TMI 1.14.0). Not deployed (no behaviour change until PR 3). #71 closed (keeping `tf_child_` keys); 16 duplicate threats from the 2026-09-22 (c) run removed from TM 02909291.
- 2026-09-22 #75 (076ad05): named LLM profiles. `llm-profiles.yaml` (gpt56cyber, mythos5, fable5, opus48) replaces LLM_PROVIDER/LLM_MODEL/LLM_API_KEY; select per invocation with `user_data.profile`, per CLI run with `--profile`, default `LLM_PROFILE`. Keys referenced by env var name and passed per LiteLLM call; bad profile or missing key fails before fan-out. AWS Terraform: `llm_profile`, `llm_api_keys` map, optional `llm_profiles_yaml` ConfigMap. NOT deployed: image + apply must ship together (write-secrets.sh now takes FILE[:VAR][=NAME]...).
- 2026-09-23 076ad05 DEPLOYED to AWS (tmi-eks, tmi-tf/tmi-tf-wh): default profile gpt56cyber; Secret holds OPENAI_CYBER_API_KEY + ANTHROPIC_API_KEY; startup logs all 4 profiles usable; health 200. Prod profile tests not yet run.
- 2026-09-23 #75 prod profile tests on TM 02909291 (image 076ad05): gpt56cyber 20 threats; mythos5 (claude-mythos-5) 27 threats; unknown profile name fails before fan-out, no clone. Note/diagram metadata carries llm-profile.
- 2026-09-23 #76 (7def4cf): cancel from TMI (#54 closed). The worker polls `GET /webhook-deliveries/{id}` (every 30 s at most; errors let the job run on); `cancelled` aborts the whole invocation; callback 409 is benign; abort() no longer sends `failed` after the invocation already closed.
- 2026-09-23 #81 (714b703): status callbacks had been no-ops since TMI #194 dropped `callback_url` (#80). They now POST to `/webhook-deliveries/{delivery id}/status` (`status_message`); accepted addon.invoked deliveries answer `X-TMI-Callback: async` (without it TMI marks them `delivered`, terminal, and every callback gets 409); each status-note line is sent as an `in_progress` update (TMI fails deliveries after 15 min of silence). #77: threat metadata set after create (TMI drops `ThreatInput.metadata`; reported to the tmi agent). #78: artifact titles timestamped per run (webhook runs now create new notes/diagram per run). #79 mitigation: the phase-3b prompt lists every allowed CWE ID (CWE-699 non-category + CWE-1446 AI/ML members 1039/1426/1427/1434); #79 stays open for the long-term fix.
- 2026-09-23 714b703 DEPLOYED to AWS; health 200. Live cancel test: delivery 01a0ced6 showed `in_progress` "Phase 1 (Inventory) started"; DELETE at 15:16:58Z; the worker aborted at the next checkpoint (15:17:35, after phase 1, $1.34), status note "Aborted: cancelled in TMI", the final `failed` callback got 409 (logged at info). A full run to `completed` on 714b703 is not yet verified.
