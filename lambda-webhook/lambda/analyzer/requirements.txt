# AWS SDK (pre-installed in Lambda, but listed for local testing)
boto3>=1.28.0

# HTTP requests (for OAuth token exchange)
requests>=2.31.0

# LLM Provider SDKs
anthropic>=0.39.0  # Claude (Anthropic)
openai>=1.0.0      # Grok (x.ai - OpenAI-compatible API)
google-cloud-aiplatform>=1.38.0  # Gemini (Google Vertex AI)
google-auth>=2.23.0              # Google authentication

# TMI Client dependencies (from tmi_tf)
python-dotenv>=1.0.0
gitpython>=3.1.40
PyGithub>=2.1.1

# TMI Python client dependencies
certifi
python-dateutil
setuptools
six
urllib3
